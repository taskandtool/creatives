# creatives/

The board. One folder per creative under the status it is in:
`generated/`, `approved/`, `scheduled/`, `posted/`, `rejected/`.
`CREATIVES.md` at the app root is the contract: the folder's contents, the
record's fields, how a creative moves, and how a portal or a publisher
reads and answers it. `scripts/status.py list` prints the board.
